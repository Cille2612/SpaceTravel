<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Space History</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="styles/main.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>

<body>
  <div class="space"></div>

  <div class="container px-4">
    <header class="py-5">
      <h1>The History of Space Travel</h1>
      <p class="lead size_m">
        This is a small practice project designed to master <strong>API integration</strong>. 
        It focuses on fetching real-time data from the SpaceX database and rendering it 
        dynamically into a visual timeline.
      </p>
    </header>

    <main class="containerLine clearfix">
      <% if (locals.historyData) { %>
        <% historyData.forEach(event => { %>
          <div class="history-card">
            <h2><%= event.title %></h2>
            <p><strong>Date:</strong> <%= new Date(event.event_date_utc).toLocaleDateString() %></p>
            <p><%= event.details %></p>
            
            <% if (event.links.wikipedia) { %>
              <a href="<%= event.links.wikipedia %>" target="_blank" class="ghost-button">
                More Information
              </a>
            <% } %>
          </div>
        <% }); %>

        <div class="history-card">
          <h2>Today</h2>
          <p><strong>Date:</strong> <%= new Date().toLocaleDateString() %></p>
          <p>The journey continues! Today, we are writing our own history in the space project.</p>
        </div>

      <% } else { %>
        <p class="text-center">No data available at the moment.</p>
      <% } %>
    </main>

    <div style="clear: both;"></div>

    <section class="py-5 footer-info size_m">
      <p>
        All information about the spaceflight history shown comes from the official SpaceX database. 
        The historical events are integrated via the 
        <a href="https://api.spacexdata.com/v3/history" target="_blank" rel="noopener noreferrer">SpaceX API</a>. 
        Further detailed information about rockets, ships, launches, and missions is also available in the official SpaceX 
        <a href="https://docs.spacexdata.com/#9f1dfdc0-fbe8-4ae5-9209-7f3d649a627c" target="_blank" rel="noopener noreferrer">API documentation</a>.
      </p>
    </section>
  </div> <footer class="text-center py-4">
    <hr style="border-color: rgba(255,255,255,0.1); width: 50%; margin: 20px auto;">
    <p>Website by Cille - ©<%= new Date().getFullYear() %></p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>